'use client'

import styled from 'styled-components';

export const BodyBox = styled.p`
  color: white;
  background-color: rgba(30, 30, 30, 0.95); // Dark gray with opacity
  border: 1px solid #F77067;
  box-shadow: inset 0 0 0 5px black; // Inner black border
  padding: 2em;
  text-align: left;
`;