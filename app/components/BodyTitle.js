'use client'

import styled from 'styled-components';

export const BodyTitle = styled.h1`
  color: white;
  font-size: 1.5em;
  background-color: rgba(30, 30, 30, 0.95); // Dark gray with opacity
  border: 1px solid #F77067;
  box-shadow: inset 0 0 0 8px black; // Inner black border
  padding: 1em 3em;
  text-align: left;
`;